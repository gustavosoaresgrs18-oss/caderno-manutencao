# ── Capacitor ──
# Nada aqui é fonte: tudo é gerado por `npm run build` e `npx cap sync`.
# Subir isto pro GitHub só engorda o repositório e cria conflito de merge.
node_modules/
www/

# A pasta android/ é gerada por `npx cap add android`. Ela PODE ser versionada
# quando você começar a editar coisas nativas à mão (AndroidManifest, ícones,
# assinatura). Enquanto for 100% gerada, deixe fora.
# ⚠️ Quando começar a versionar, NUNCA suba a chave de assinatura (.jks/.keystore)
# nem o arquivo com as senhas dela — quem tem a chave publica atualização no seu
# lugar, e perder a chave significa não conseguir mais atualizar o app.
android/

# chaves de assinatura — nunca, em hipótese nenhuma
*.jks
*.keystore
key.properties

# lixo de sistema
.DS_Store
Thumbs.db
